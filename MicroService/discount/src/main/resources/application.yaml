server:
  port: 8080
  address: localhost

#logging:
#  level:
#    root: debug

spring:
  datasource:
    username: root
    password: Mm@123456
    url: jdbc:mysql://localhost:3306/MicroService
  jpa:
    database: mysql
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    data:
      jpa:
        repositories:
          enabled: true